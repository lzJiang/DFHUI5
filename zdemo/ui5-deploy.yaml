# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: zdemo
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: 0M69GET_SAML_ASSERTION
          url: https://my200830-api.s4hana.sapcloud.cn
        app:
          name: ZDEMOFIORI
          description: zdemofiori
          package: ZDEV
          transport: NMWK900027
        exclude:
          - /test/
